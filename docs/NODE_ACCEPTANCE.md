# Linux Node acceptance

Status: implementation and native acceptance are in progress. Neither Linux
architecture is yet recorded as passing the complete narrowed milestone.

Campaign pins: Node 26.10.0, npm 11.19.1, Lean 4.34.1; native Ubuntu 24.04 runners
on x86-64 and ARM64. The same packed candidate must pass both architectures.

The package is not published to npm. This report will record the exact source
revision, package SHA-256, runtime identities, installation and deployment
measurements, and links to preserved CI results. Older broad-suite results do
not establish acceptance of this candidate.
