**Bundle-size reduction — 2026-09-26:** Reachability-based linking, size optimization and architecture-specific host packaging are implemented and pushed. Ordinary Lean source and CLI commands are unchanged.

**Verified:** Candidate `.34` packed reproducibly. Native Linux x86-64 passed 49 command checks and eight isolated deployments: Hello 3.21 MB, JSON 2.88 MB, filesystem 3.30 MB. A thousand unused functions produce byte-identical Wasm. The full runtime-evaluation fallback also passes and remains 2.39 GB. Local HTTP passed all 20 unchanged tests; 113 focused tests pass. No OOM. Earlier disk-reserve aborts remain separate in [evidence](evidence/bundle-size-2026-09-26.json).

**Remaining:** Native ARM64 is running against the same archive, followed by candidate retention and final documentation. The accepted `.32` archive remains unchanged; no npm publication. ETA: this CI campaign if ARM64 passes; otherwise not yet estimable. Broader API parity and compiler-distribution minimization remain separate work.
