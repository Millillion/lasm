**Bundle-size reduction — 2026-09-26:** Candidate `.34` shrinks ordinary applications through linker reachability and architecture-specific packaging. Native Linux x86-64 passed 49 command checks and eight isolated deployments: Hello 3.21 MB, JSON 2.88 MB, filesystem 3.30 MB. Unused functions produce byte-identical Wasm. The 2.39 GB runtime-evaluation fallback also passes. Local HTTP passed 20 tests; 113 focused tests pass.

**ARM64:** Seven small applications passed before a proactive memory stop during runtime evaluation; zero OOM. Inactive file-cache pages dominated the stop. CI advice now covers completed native comparison artifacts, verified by four small filesystem controls. Product bytes, assertions, limits and deployment files are unchanged. The [abort evidence](evidence/bundle-size-arm64-resource-abort-2026-09-26.json) is preserved.

**Remaining:** Recheck the identical archive on both native architectures, retain it, and finalize documentation. No npm publication. ETA: one native recheck campaign if the corrected resource profile passes; otherwise not yet estimable.
