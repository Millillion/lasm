{
  "version": "6.0.9",
  "patchSha256": "f8b31335a97fc2dfd11cfc94ee1787d0b1aff6435ffd765a5613a9553f2e60dc",
  "files": [
    {
      "path": "tools/link.py",
      "replacements": [
        {
          "before": "  set_max_memory()\n\n  # check if we can address the 2GB mark and higher.\n  if not settings.MEMORY64 and settings.MAXIMUM_MEMORY > 2 * 1024 * 1024 * 1024:\n    settings.CAN_ADDRESS_2GB = 1\n\n  if settings.MAX_WEBGL_VERSION >= 2:\n",
          "after": "  set_max_memory()\n\n  # check if we can address the 2GB mark and higher.\n  # Lowered memory64 still indexes wasm32 memory from JavaScript. Its heap\n  # shifts must be unsigned, just as for ordinary wasm32 above the 2GB mark.\n  if settings.MEMORY64 != 1 and settings.MAXIMUM_MEMORY > 2 * 1024 * 1024 * 1024:\n    settings.CAN_ADDRESS_2GB = 1\n\n  if settings.MAX_WEBGL_VERSION >= 2:\n"
        },
        {
          "before": "    # main module to avoid creating new invoke functions at runtime.\n    imports = set(imports)\n    imports = {i for i in imports if not i.startswith('invoke_')}\n    weak_imports = webassembly.get_weak_imports(dylib)\n    strong_imports = sorted(imports.difference(weak_imports))\n    logger.debug('Adding symbols requirements from `%s`: %s', dylib, imports)\n",
          "after": "    # main module to avoid creating new invoke functions at runtime.\n    imports = set(imports)\n    imports = {i for i in imports if not i.startswith('invoke_')}\n    # Relocation bases belong to each side module and are provided by the\n    # dynamic loader, not exported by the main module's C/JavaScript libraries.\n    imports.difference_update({'__memory_base', '__table_base',\n                               '__memory_base32', '__table_base32'})\n    weak_imports = webassembly.get_weak_imports(dylib)\n    strong_imports = sorted(imports.difference(weak_imports))\n    logger.debug('Adding symbols requirements from `%s`: %s', dylib, imports)\n"
        }
      ],
      "patchedSha256": "2f143e5ca6d8aa560f5b2ea5e1d31a1ea897d068ea7778e50aa07d84ff10e481",
      "originalSha256": "c5332e4fbdfbc9c7223c57ae8b34bcc28921fef2daefd8470d0583e7bfdf0efc"
    },
    {
      "path": "src/lib/libdylink.js",
      "replacements": [
        {
          "before": "        if ('__start_em_asm' in moduleExports) {\n          var start = moduleExports['__start_em_asm'].value;\n          var stop = moduleExports['__stop_em_asm'].value;\n#if CAN_ADDRESS_2GB\n          start >>>= 0;\n          stop >>>= 0;\n#else\n",
          "after": "        if ('__start_em_asm' in moduleExports) {\n          var start = moduleExports['__start_em_asm'].value;\n          var stop = moduleExports['__stop_em_asm'].value;\n#if MEMORY64\n          {{{ from64('start') }}}\n          {{{ from64('stop') }}}\n#elif CAN_ADDRESS_2GB\n          start >>>= 0;\n          stop >>>= 0;\n#else\n"
        },
        {
          "before": "#if ASSERTIONS\n    assert(lib, `Tried to dlsym() from an unopened handle: ${handle}`);\n#endif\n    newSymIndex = Object.keys(lib.exports).indexOf(symbol);\n    if (newSymIndex == -1 || lib.exports[symbol].stub) {\n      dlSetError(`Tried to lookup unknown symbol \"${symbol}\" in dynamic lib: ${lib.name}`)\n      return 0;\n    }\n",
          "after": "#if ASSERTIONS\n    assert(lib, `Tried to dlsym() from an unopened handle: ${handle}`);\n#endif\n#if WASM_BIGINT\n    // Most interpreter lookups miss or find a function already in the table.\n    // Preserve own/enumerable semantics without scanning the export dictionary.\n    if (!Object.prototype.propertyIsEnumerable.call(lib.exports, symbol) || lib.exports[symbol].stub) {\n#else\n    newSymIndex = Object.keys(lib.exports).indexOf(symbol);\n    if (newSymIndex == -1 || lib.exports[symbol].stub) {\n#endif\n      dlSetError(`Tried to lookup unknown symbol \"${symbol}\" in dynamic lib: ${lib.name}`)\n      return 0;\n    }\n"
        },
        {
          "before": "        // function the second argument will not be needed.  If it's a JS\n        // function we rely on the `sig` attribute being set based on the\n        // `<func>__sig` specified in library JS file.\n        result = addFunction(result, result.sig);\n#if DYLINK_DEBUG\n        dbg('adding symbol to table:',  symbol);\n",
          "after": "        // function the second argument will not be needed.  If it's a JS\n        // function we rely on the `sig` attribute being set based on the\n        // `<func>__sig` specified in library JS file.\n#if WASM_BIGINT\n        // Worker catch-up still needs the exact current enumeration index.\n        newSymIndex = Object.keys(lib.exports).indexOf(symbol);\n#endif\n        result = addFunction(result, result.sig);\n#if DYLINK_DEBUG\n        dbg('adding symbol to table:',  symbol);\n"
        }
      ],
      "patchedSha256": "711eba55c77409aad5302a05ea48e3d1c15a04dbb0221f34cb80db0979b79d8c",
      "originalSha256": "37342422027bba9f80c23a8a426217e7cffa3ae2a0a2a1fcdf5416c2ec4316e8"
    },
    {
      "path": "src/lib/libpthread.js",
      "replacements": [
        {
          "before": "    worker.onmessage = (e) => {\n#if ASSERTIONS\n      var cmd = e.data.cmd;\n      err(`received \"${cmd}\" command from terminated worker: ${worker.workerID}`);\n#endif\n    };\n",
          "after": "    worker.onmessage = (e) => {\n#if ASSERTIONS\n      var cmd = e.data.cmd;\n      // Joined/detached pthreads may finish concurrently with process exit.\n      // Their normal completion packets can already be queued at terminate().\n      if (cmd === {{{ CMD_CLEANUP_THREAD }}} || cmd === {{{ CMD_MARK_AS_FINISHED }}}) return;\n      err(`received \"${cmd}\" command from terminated worker: ${worker.workerID}`);\n#endif\n    };\n"
        },
        {
          "before": "  _emscripten_thread_cleanup: (thread) => {\n",
          "after": "  $cleanupThreadProxied__internal: true,\n  $cleanupThreadProxied__proxy: 'sync',\n  $cleanupThreadProxied__deps: ['$cleanupThread'],\n  $cleanupThreadProxied: (thread) => cleanupThread(thread),\n\n  _emscripten_thread_cleanup__deps: ['$cleanupThreadProxied', 'pthread_self'],\n  _emscripten_thread_cleanup: (thread) => {\n"
        },
        {
          "before": "    if (!ENVIRONMENT_IS_PTHREAD) cleanupThread(thread);\n    else postMessage({ cmd: {{{ CMD_CLEANUP_THREAD }}}, thread });",
          "after": "    if (!ENVIRONMENT_IS_PTHREAD) cleanupThread(thread);\n    // A joining thread must reclaim the old stack before returning, so an\n    // immediate replacement thread can reuse its reservation. A detached\n    // thread clears pthread_self before requesting its own cleanup and must\n    // keep the asynchronous path: its proxy wait data lives on that stack.\n    else if (_pthread_self()) cleanupThreadProxied(thread);\n    else postMessage({ cmd: {{{ CMD_CLEANUP_THREAD }}}, thread });"
        }
      ],
      "patchedSha256": "a447ff4703487d57998372b675efd85c98897c200f93a4c803bd3910528f2f44",
      "originalSha256": "9b762d32a9473f720fe75f64a5bd56108a91654b9833cc8842553c631288d6a9"
    },
    {
      "path": "emscripten-version.txt",
      "originalSha256": "6bf7f89a794c69175d3d2b6616049da0e4f9190af284aa480992e29332ec6c47",
      "patchedSha256": "243d1b6b2a45198d87048f5e81fba5bc8cf540c3bb54fb1d9ebd1df190e21014",
      "replacements": [
        {
          "before": "6.0.9-git\n",
          "after": "\"6.0.9\"\n"
        }
      ]
    },
    {
      "path": "tools/utils.py",
      "originalSha256": "0ab7e53b2f0e05b90cb3c4e01f282c93834d6439af518afbb79e31719100dc8c",
      "patchedSha256": "01bfd7c3d8dcf19d86ece199860cc6c29036194a292935f5eab89021770c45b9",
      "replacements": [
        {
          "before": "def run_process(cmd, check=True, input=None, *args, **kw):\n",
          "after": "def python_compiler_command(cmd):\n  # Lasm's private compiler dependency must not write bytecode into its verified\n  # installation. Bypass the platform entrypoint wrappers, whose -E drops the\n  # PYTHONDONTWRITEBYTECODE environment variable, for internal compiler calls.\n  if not isinstance(cmd, (list, tuple)) or not cmd:\n    return cmd\n  program = os.path.abspath(cmd[0])\n  name, extension = os.path.splitext(os.path.basename(program))\n  if (os.path.normcase(os.path.dirname(program)) == os.path.normcase(__rootpath__)\n      and name in {'emcc', 'em++', 'emar', 'emranlib', 'embuilder'}\n      and extension in {'', '.exe', '.cmd', '.bat'}):\n    return [sys.executable, '-B', '-E', '-s', path_from_root(name + '.py'), *cmd[1:]]\n  return cmd\n\n\ndef run_process(cmd, check=True, input=None, *args, **kw):\n"
        },
        {
          "before": "  ret = subprocess.run(cmd, check=check, input=input, *args, **kw)\n",
          "after": "  cmd = python_compiler_command(cmd)\n  ret = subprocess.run(cmd, check=check, input=input, *args, **kw)\n"
        }
      ]
    },
    {
      "path": "tools/shared.py",
      "originalSha256": "ffa65e36574d213aa71ec5b540db47bdc588cf7483c717136e06d320acc04edd",
      "patchedSha256": "72a72ef63776f895c6e51f1f67341eedcf884e9c5f2bb938d4e7fd6e023dfbc4",
      "replacements": [
        {
          "before": "      proc = subprocess.Popen(commands[i], stdout=stdout, stderr=None, env=env, cwd=cwd)\n",
          "after": "      proc = subprocess.Popen(utils.python_compiler_command(commands[i]), stdout=stdout, stderr=None, env=env, cwd=cwd)\n"
        },
        {
          "before": "def exec_process(cmd):\n  print_compiler_stage(cmd)\n  utils.exec(cmd)\n",
          "after": "def exec_process(cmd):\n  print_compiler_stage(cmd)\n  # Keep Clang argument batches below the Windows CreateProcess limit, including\n  # the expanded source paths from internal SDK response files.\n  if utils.WINDOWS and len(subprocess.list2cmdline(cmd).encode(\"utf-16-le\")) // 2 > 24000:\n    from . import response_file\n    cmd = [cmd[0], \"@\" + response_file.create_response_file(cmd[1:], TEMP_DIR)]\n  utils.exec(cmd)\n"
        }
      ]
    },
    {
      "path": "system/lib/emmalloc.c",
      "originalSha256": "00eb577cbef51a3359c309f45a7c99a814b8c6151ab75c9a3c78a56f0169146b",
      "patchedSha256": "a2eb6a0686335e3120edb59f31812b9349591042f3b29a74f59daaf99d0984ae",
      "replacements": [
        {
          "before": "#define FREE_REGION_FLAG 0x1u",
          "after": "#define FREE_REGION_FLAG ((size_t)1)"
        },
        {
          "before": "#define MAX_ALLOC_SIZE 0xFFFFFFC7u",
          "after": "// Keep region bookkeeping and signed sbrk growth within the pointer width.\n#define MAX_ALLOC_SIZE (MIN(SIZE_MAX, INT64_MAX) - 3 * sizeof(Region) - MALLOC_ALIGNMENT)"
        },
        {
          "before": "typedef struct RootRegion {\n  uint32_t size;",
          "after": "typedef struct RootRegion {\n  size_t size;"
        },
        {
          "before": "  if (allocSize < 128) return (allocSize >> 3) - 1;\n  int clz = __builtin_clz(allocSize);",
          "after": "  if (allocSize < 128) return (allocSize >> 3) - 1;\n  // Larger memory64 regions all belong in the final bucket. Clamp before\n  // the 32-bit leading-zero operation so high size bits cannot be lost.\n  if (allocSize >= 134217728) return NUM_FREE_BUCKETS - 1;\n  int clz = __builtin_clz((unsigned)allocSize);"
        },
        {
          "before": "  if (alignment > MALLOC_ALIGNMENT) {\n    numBytesToClaim += alignment;",
          "after": "  if (alignment > MALLOC_ALIGNMENT) {\n    if (alignment > MAX_ALLOC_SIZE - size) return 0;\n    numBytesToClaim += alignment;"
        },
        {
          "before": "  size_t bytes = num*size;\n",
          "after": "  if (size && num > SIZE_MAX / size) return 0;\n  size_t bytes = num*size;\n"
        },
        {
          "before": "        ++freeMemorySizeMap[31-__builtin_clz(freeDynamicMemory)];",
          "after": "        // The public histogram has 32 entries; its last bucket includes\n        // memory64 regions of 2 GiB and above.\n        int index = freeDynamicMemory >= ((size_t)1 << 31) ? 31\n                    : 31 - __builtin_clz((unsigned)freeDynamicMemory);\n        ++freeMemorySizeMap[index];"
        },
        {
          "before": "  // Claim memory via sbrk\n  assert((int64_t)numBytes >= 0);",
          "after": "  // Reject impossible reservations before signed sbrk address arithmetic.\n  if (numBytes > emscripten_get_heap_max()) return false;\n  // Claim memory via sbrk\n  assert((int64_t)numBytes >= 0);"
        },
        {
          "before": "  if (size > MAX_ALLOC_SIZE) {\n#ifdef EMMALLOC_VERBOSE\n    MAIN_THREAD_ASYNC_EM_ASM(out('Allocation failed: attempted allocation size is too large: ' + Number($0) + 'bytes! (negative integer wraparound?)'), size);\n#endif\n    return 0;\n  }\n\n  alignment = validate_alloc_alignment(alignment);",
          "after": "  if (size > MAX_ALLOC_SIZE || alignment > MAX_ALLOC_SIZE) {\n#ifdef EMMALLOC_VERBOSE\n    MAIN_THREAD_ASYNC_EM_ASM(out('Allocation failed: attempted allocation size is too large: ' + Number($0) + 'bytes! (negative integer wraparound?)'), size);\n#endif\n    return 0;\n  }\n\n  alignment = validate_alloc_alignment(alignment);"
        }
      ]
    },
    {
      "path": "system/lib/pthread/pthread_create.c",
      "originalSha256": "06e10210681c2f0b92c1e98f9185a4faabcd57f053e949c65ff9ad74b12b65cf",
      "patchedSha256": "2d57cff529827c3b77b7104b68c658a30ad4b1b99da435d09ae0dac86bd08082",
      "replacements": [
        {
          "before": "    size += guard_size + attr._a_stacksize + STACK_ALIGN - 1;\n",
          "after": "    // Reject impossible stack sizes before addition can wrap.\n    if (guard_size > SIZE_MAX - size - (STACK_ALIGN - 1) ||\n        attr._a_stacksize > SIZE_MAX - size - (STACK_ALIGN - 1) - guard_size)\n      return EAGAIN;\n    size += guard_size + attr._a_stacksize + STACK_ALIGN - 1;\n"
        },
        {
          "before": "  unsigned char* block = emscripten_builtin_malloc(size);\n  memset(block, 0, zero_size);",
          "after": "  unsigned char* block = emscripten_builtin_malloc(size);\n  if (!block) return EAGAIN;\n  memset(block, 0, zero_size);"
        }
      ]
    },
    {
      "path": "tools/config.py",
      "originalSha256": "c2614f51eb78480eb206651c15388d0977ec90576c0cef87fe0a753949d54954",
      "patchedSha256": "592a97b529c6f30131348723ce89f1652dc6019ef1ee3bec522ba2b5257ac7af",
      "replacements": [
        {
          "before": "  if os.environ.get('EMSDK_PYTHON'):\n    os.environ['EMSDK_PYTHON'] = sys.executable\n",
          "after": "  if os.environ.get('EMSDK_PYTHON'):\n    # Keep a private launcher (including its Python isolation flags) when\n    # normalizing paths for configure/make and other recursive tool calls.\n    os.environ['EMSDK_PYTHON'] = os.path.abspath(os.environ['EMSDK_PYTHON'])\n"
        }
      ]
    }
  ],
  "archiveVersion": "6.0.9-git",
  "versionNormalization": "The official emsdk installer writes the stable release version resolved from this archive hash into emscripten-version.txt.",
  "internalCompilerLaunches": "Use private Python directly with -B -E -s for recursive SDK tools; avoid shell-wrapper bytecode writes and retain strict whole-cache verification.",
  "windowsCommandLines": "Convert expanded native compiler commands above 24000 UTF-16 code units into the SDK's ordinary LLVM response files before CreateProcess.",
  "memory64Allocator": "Use pointer-width allocator metadata, masks and size bounds; clamp bucket lookup before 32-bit leading-zero operations and reject overflowing aligned/calloc requests.",
  "pthreadAllocation": "Return EAGAIN for overflowing or failed stack allocations before writing thread metadata; preserve requested stack sizes.",
  "joinedThreadCleanup": "Synchronously reclaim joined-thread storage before returning; detached self-cleanup remains asynchronous after pthread_self is cleared.",
  "pythonLauncherNormalization": "Resolve the configured Python launcher to an absolute path without discarding its isolation flags during configure/make child launches."
}
