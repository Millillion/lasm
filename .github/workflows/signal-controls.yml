name: Native application signal controls
on:
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: signal-controls-${{ github.ref }}
  cancel-in-progress: false

# Source controls on free standard public runners, with no Actions caches or
# uploaded artifacts. Native compilation here is not shipped-binary acceptance.
jobs:
  signals:
    if: github.event.repository.visibility == 'public'
    name: Signal routing ${{ matrix.platform }}
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 10
    strategy:
      fail-fast: false
      max-parallel: 2
      matrix:
        include:
          - { platform: linux-x64, runner: ubuntu-24.04, arch: x64 }
          - { platform: linux-arm64, runner: ubuntu-24.04-arm, arch: arm64 }
    env:
      LASM_EXPECT_PLATFORM: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 26.10.0
          architecture: ${{ matrix.arch }}
          package-manager-cache: false
      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: 1.4.2
          no-cache: true
      - uses: denoland/setup-deno@22d081ff2d3a40755e97629de92e3bcbfa7cf2ed # v2.0.5
        with:
          deno-version: 2.9.7
          cache: false
      - run: npm ci --omit=dev --ignore-scripts --no-audit --no-fund
      - name: Check native engines and retain exact paths
        run: |
          node -e "require('node:assert/strict').equal(process.platform+'-'+process.arch,process.env.LASM_EXPECT_PLATFORM)"
          deno eval "if(Deno.version.deno!=='2.9.7'||process.platform+'-'+process.arch!==process.env.LASM_EXPECT_PLATFORM)throw Error('Incorrect engine'); console.log(Deno.version)"
          bun -e "if(Bun.version!=='1.4.2'||process.platform+'-'+process.arch!==process.env.LASM_EXPECT_PLATFORM)throw Error('Incorrect engine'); console.log(process.versions)"
          printf 'LASM_TEST_DENO=%s\n' "$(command -v deno)" >> "$GITHUB_ENV"
          printf 'LASM_TEST_BUN=%s\n' "$(command -v bun)" >> "$GITHUB_ENV"
      - name: Enable process-tree guard
        run: |
          sudo loginctl enable-linger "$(id -un)"
          printf 'XDG_RUNTIME_DIR=/run/user/%s\n' "$(id -u)" >> "$GITHUB_ENV"
          printf 'DBUS_SESSION_BUS_ADDRESS=unix:path=/run/user/%s/bus\n' "$(id -u)" >> "$GITHUB_ENV"
      - name: Build native control helper
        run: |
          ulimit -c 0
          node scripts/full-lean/run-bounded.mjs --memory-mib 1024 --report .work/signal-helper-build-resources.json -- node scripts/build-signal-helper.mjs --native
      - name: Guarded signal controls
        run: |
          ulimit -c 0
          node scripts/full-lean/run-bounded.mjs --memory-mib 1024 --report .work/signal-controls-resources.json -- /usr/bin/env LASM_TEST_BUN="$LASM_TEST_BUN" LASM_TEST_DENO="$LASM_TEST_DENO" LASM_REQUIRE_SIGNAL_ENGINES=1 node --max-old-space-size=128 --test --test-concurrency=1 test/node-signal.test.mjs test/application-signals.test.mjs test/application-output.test.mjs
      - name: Preserve guard and helper receipts in logs
        if: always()
        run: |
          node -e "const f=require('node:fs');for(const p of ['.work/signal-helper-build-resources.json','.work/signal-controls-resources.json','.cache/native-host/signals/manifest.json'])if(f.existsSync(p))console.log(p+'\n'+f.readFileSync(p,'utf8'))"
